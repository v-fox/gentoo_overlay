--- sixad/Makefile	2010-01-23 16:21:54.000000000 +0100
+++ sixad/Makefile	2011-02-18 01:44:47.004000137 +0100
@@ -24,29 +24,30 @@
 
 install-system:
 	#Creating needed directories
-	mkdir -p /lib/udev/rules.d/
-	mkdir -p /var/lib/sixad/
+	mkdir -p ${DESTDIR}/usr/bin/
+	mkdir -p ${DESTDIR}/usr/sbin/
+	mkdir -p ${DESTDIR}/usr/share/hal/fdi/policy/20thirdparty/
+	mkdir -p ${DESTDIR}/usr/share/man/man1/
+	mkdir -p ${DESTDIR}/lib/udev/rules.d/
+	mkdir -p ${DESTDIR}/var/lib/sixad/
+	mkdir -p ${DESTDIR}/etc/default/
 
 	#Installing binaries
-	cp sixad /usr/bin/
-	cp hcid/hcid /usr/sbin/
-	cp bins/hidraw-dump /usr/sbin/
-	cp bins/sixad-bin /usr/sbin/
-	cp bins/sixad-raw /usr/sbin/
-	cp bins/sixad-uinput-sixaxis /usr/sbin/
-	cp bins/sixpair /usr/sbin/
-	cp bins/sixpair-kbd /usr/sbin/
+	cp sixad ${DESTDIR}/usr/bin/
+	cp hcid/hcid ${DESTDIR}/usr/sbin/
+	cp bins/hidraw-dump ${DESTDIR}/usr/sbin/
+	cp bins/sixad-bin ${DESTDIR}/usr/sbin/
+	cp bins/sixad-raw ${DESTDIR}/usr/sbin/
+	cp bins/sixad-uinput-sixaxis ${DESTDIR}/usr/sbin/
+	cp bins/sixpair ${DESTDIR}/usr/sbin/
+	cp bins/sixpair-kbd ${DESTDIR}/usr/sbin/
 
 	#Installing misc files
-	cp 98-sixad.rules /lib/udev/rules.d/
-	cp default/sixad /etc/default/
-	cp init/sixad /etc/init.d/
-	cp x11-sony-keypad.fdi /usr/share/hal/fdi/policy/20thirdparty/
-	cp mans/* /usr/share/man/man1/
-	ln -s /etc/init.d/sixad /etc/rc0.d/K10sixad || true
-	ln -s /etc/init.d/sixad /etc/rc1.d/K10sixad || true
-	ln -s /etc/init.d/sixad /etc/rc6.d/K10sixad || true
+	cp 98-sixad.rules ${DESTDIR}/lib/udev/rules.d/
+	cp default/sixad ${DESTDIR}/etc/default/
+	cp x11-sony-keypad.fdi ${DESTDIR}/usr/share/hal/fdi/policy/20thirdparty/
+	cp mans/* ${DESTDIR}/usr/share/man/man1/
 
 	@echo "Finished"
 
 
--- qtsixa/Makefile	2010-01-27 02:02:53.000000000 +0100
+++ qtsixa/Makefile	2011-02-18 02:23:29.884000708 +0100
@@ -5,25 +5,38 @@
 	@echo "QtSixA doesn't need to be compiled :)"
 
 install:
-	cp ./doc/* /usr/share/doc/qtsixa/
-	cp ./manual/* /usr/share/doc/qtsixa/manual/
-	cp ./game-profiles/* /usr/share/qtsixa/game-profiles/
-	cp ./gui/* /usr/share/qtsixa/gui/
-	cp ./pics/* /usr/share/qtsixa/pics/
-	cp ./lang/* /usr/share/qtsixa/lang/
-	cp ./icons/* /usr/share/qtsixa/icons/
-	cp ./sixaxis-profiles/* /usr/share/qtsixa/sixaxis-profiles/
-	cp ./sixa /usr/bin/
-	cp ./sixa-lq /usr/bin/
-	cp ./sixa-notify /usr/bin/
-	cp ./qtsixa /usr/bin/
-	cp ./qtsixa.conf /usr/share/qtsixa/
-	cp ./qtsixa.desktop /usr/share/applications/
-	cp ./qtsixa.xpm /usr/share/pixmaps/
-	cp ./profiles.list.bu /usr/share/qtsixa/
-	cp ./features.html /usr/share/qtsixa/
-	cp ./sixa-notify.desktop /usr/share/qtsixa/
-	if [ ! -f /usr/share/qtsixa/profiles.list.bu ]; then cp ./profiles.list.bu /usr/share/qtsixa/profiles.list; fi
+	mkdir -p ${DESTDIR}/usr/share/doc/qtsixa/
+	mkdir -p ${DESTDIR}/usr/share/doc/qtsixa/manual/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/game-profiles/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/gui/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/pics/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/lang/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/icons/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/sixaxis-profiles/
+	mkdir -p ${DESTDIR}/usr/bin/
+	mkdir -p ${DESTDIR}/usr/sbin/
+	mkdir -p ${DESTDIR}/usr/share/qtsixa/
+	mkdir -p ${DESTDIR}/usr/share/applications/
+	mkdir -p ${DESTDIR}/usr/share/pixmaps/
+	cp ./doc/* ${DESTDIR}/usr/share/doc/qtsixa/
+	cp ./manual/* ${DESTDIR}/usr/share/doc/qtsixa/manual/
+	cp ./game-profiles/* ${DESTDIR}/usr/share/qtsixa/game-profiles/
+	cp ./gui/* ${DESTDIR}/usr/share/qtsixa/gui/
+	cp ./pics/* ${DESTDIR}/usr/share/qtsixa/pics/
+	cp ./lang/* ${DESTDIR}/usr/share/qtsixa/lang/
+	cp ./icons/* ${DESTDIR}/usr/share/qtsixa/icons/
+	cp ./sixaxis-profiles/* ${DESTDIR}/usr/share/qtsixa/sixaxis-profiles/
+	cp ./sixa ${DESTDIR}/usr/bin/
+	cp ./sixa-lq ${DESTDIR}/usr/bin/
+	cp ./sixa-notify ${DESTDIR}/usr/bin/
+	cp ./qtsixa ${DESTDIR}/usr/bin/
+	cp ./qtsixa.conf ${DESTDIR}/usr/share/qtsixa/
+	cp ./qtsixa.desktop ${DESTDIR}/usr/share/applications/
+	cp ./qtsixa.xpm ${DESTDIR}/usr/share/pixmaps/
+	cp ./profiles.list.bu ${DESTDIR}/usr/share/qtsixa/
+	cp ./features.html ${DESTDIR}/usr/share/qtsixa/
+	cp ./sixa-notify.desktop ${DESTDIR}/usr/share/qtsixa/
+	if [ ! -f ${DESTDIR}/usr/share/qtsixa/profiles.list.bu ]; then cp ./profiles.list.bu /usr/share/qtsixa/profiles.list; fi
 
 ### Note to packagers:
 #  The real "profiles.list" should not be installed by default, only if it is not found.
